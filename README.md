# PineconeOS Distro Repo

VCS for PineconeOS binary packages (.pine) and PINEINDEX.

Source repo: PineconeOS (kernel + distro build)
Generated via /tmp/opencode/build_pine.lua and pkg_register.sh

Packages are built from /tmp/opencode/*-pkg/ dirs and registered in both
/tmp/PineconeOS/usr/share/pine/repo and /tmp/knuck-fsroot/usr/share/pine/repo.

Update: rebuild package, re-run pkg_register.sh, then commit here.
